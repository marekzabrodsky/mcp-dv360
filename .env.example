# DV360 MCP Server Environment Configuration

# Authentication Method 1: Service Account (Recommended for production)
# Download service account JSON key from Google Cloud Console
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# Authentication Method 2: OAuth 2.0 (For development/testing)  
# Get these from Google Cloud Console OAuth 2.0 credentials
# OAUTH_CLIENT_ID=your-oauth-client-id.apps.googleusercontent.com
# OAUTH_CLIENT_SECRET=your-oauth-client-secret
# OAUTH_REFRESH_TOKEN=your-refresh-token

# Optional Server Settings
LOG_LEVEL=INFO
MAX_RETRIES=3
TIMEOUT_SECONDS=30